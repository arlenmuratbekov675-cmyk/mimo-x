"""ORM models. Empty for Step 0 — market/history models come in later steps."""
from app.database import Base  # noqa: F401

# Intentionally no models yet. Future steps will add e.g. MarketSnapshot, Indicator, History.
